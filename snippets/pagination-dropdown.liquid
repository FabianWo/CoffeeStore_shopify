<!-- {% if collection.sort_by %}
  {% for option in collection.sort_options %}
    {% if option.value == collection.sort_by %}
      {% assign current_sort = option %}
    {% endif %}
  {% endfor %}
{% endif %}

{% if collection.default_sort_by %}
  {% for option in collection.sort_options %}
    {% if option.value == collection.default_sort_by %}
      {% assign current_sort = option %}
    {% endif %}
  {% endfor %}
{% endif %} -->

<div class="collection-filter">
  <span class="collection-filter__text">Produkte pro Seite:&nbsp;&nbsp;</span>
  <button class="collection-filter__button cart__form-quantity-button">
    <span id="displayValue">{{ current_sort.name }}</span>
    <input id="pagination-input" class="cart__quantity-input collection-filter__input" type="text"
    name="{{ current_sort.value }}"
    value="{{ current_sort.name }}">
    {{ 'sortMenuArrow.svg'| asset_url | img_tag : 'sortList Arrow', 'collection-filter__arrow' }}

    <ul class="cart__quantity-list box-lightshadow collection-filter__list">

      {% for option in collection.sort_options %}
        <li value="{{ option.name }}" name="{{ option.value }}" class="cart__quantity-list-item collection-filter__list-item">
          {{ option.name }}
        </li>
      {% endfor %}

    </ul>
    
  </button>
</div>